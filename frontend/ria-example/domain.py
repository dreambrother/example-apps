class Transaction:

	def __init__(self, id, contact, message, amount, status, date):
		self.id = id
		self.contact = contact
		self.message = message
		self.amount = amount
		self.status = status
		self.date = date